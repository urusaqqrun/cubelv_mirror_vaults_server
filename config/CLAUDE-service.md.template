# CubeLV 後端服務鍛造

你的任務是建立或修改一個 CubeLV 後端服務（Backend Service）。

## 工作目錄

- 你的 **CWD 就是 vault 根目錄**（例如 `/vaults/abc123/`）
- 服務程式碼寫到 `services/<系統提供的目錄名>/`（相對於 CWD）
- **直接 `mkdir -p services/<目錄名>` 建立目錄**
- **禁止存取 vault 根目錄以外的任何路徑**

## 目錄與命名

- 用戶訊息第一行會明確給你目標目錄，例如：`服務目錄名稱：services/telegram-bot-a1b2c3/`
- **必須使用系統提供的目錄名，不得自行更改**

## 技術規範

### 必須使用 @cubelv/service-sdk

```typescript
import { createService } from '@cubelv/service-sdk';

const { app, start } = createService({ name: 'my-service' });

// 註冊路由
app.post('/webhook/telegram', (req, res) => {
  // 處理 Telegram webhook
  res.json({ ok: true });
});

app.get('/api/status', (req, res) => {
  res.json({ status: 'running' });
});

// 啟動服務
start();
```

### 環境變數

服務運行時可用的環境變數：
- `PORT` — 服務監聽埠（由系統指派，不要硬編碼）
- `CUBELV_MEMBER_ID` — 會員 ID
- `CUBELV_SERVICE_DIR` — 服務目錄名

### 專案結構

```
services/<目錄名>/
├── package.json          ← 必須包含 @cubelv/service-sdk 依賴
├── tsconfig.json         ← TypeScript 配置
├── src/
│   └── index.ts          ← 入口檔
├── dist/
│   └── index.js          ← 編譯後入口（esbuild bundle）
└── manifest.json         ← 服務描述檔（必要）
```

### manifest.json（必要）

```json
{
  "name": "服務名稱",
  "version": "1.0.0",
  "endpoints": [
    {
      "path": "/webhook/telegram",
      "method": "POST",
      "public": true,
      "webhook_secret_header": "X-Telegram-Bot-Api-Secret-Token",
      "webhook_secret_value": "由用戶提供的 secret"
    },
    {
      "path": "/api/status",
      "method": "GET",
      "public": false
    }
  ]
}
```

- `public: true` — 不需登入即可存取（webhook 用），但必須配 `webhook_secret_header` + `webhook_secret_value`
- `public: false` — 需要登入，只有服務擁有者可存取

### 服務 URL

服務部署後的 URL 格式：
```
https://dev.cubelv.com/svc/{memberID}/{serviceDir}/{endpoint}
```

例如 Telegram webhook URL：
```
https://dev.cubelv.com/svc/abc123/telegram-bot-a1b2c3/webhook/telegram
```

## 建置流程

1. 寫好 `src/index.ts`
2. 寫好 `package.json`（含 `@cubelv/service-sdk` 和其他依賴）
3. 寫好 `manifest.json`
4. 執行 `npm install`（在服務目錄內）
5. 執行 `npx esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js`
6. 驗證 `dist/index.js` 存在

## 限制

- 每個服務最大記憶體 128MB
- idle 5 分鐘自動關閉（有新請求時自動重啟）
- 不保證長駐，不要依賴 in-memory 狀態
- 需要持久化的資料用外部服務（資料庫、Redis、S3 等）
